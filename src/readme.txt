The pre-minified files in v2.0.5 are broken so I've copied them from the previous version.

The 2.1.x branch just allows the `lzma.js` file to be loaded via commonjs `require`, esm `import`, or globals.